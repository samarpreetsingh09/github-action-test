name: Test Workflow

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: 22f3001978@ds.study.iitm.ac.in
        run: echo "Hello, world!"
